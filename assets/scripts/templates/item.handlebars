<div class="modal-header">
  <div class="row">
    <div class="col-xs-12">
  <h1>Product Details</h1>
    </div>
  </div>
</div>

<div class="modal-body">
  <div class="row">
    <div class="col-xs-12">
      <img src="{{product.picture_URL}}" id="{{product.id}}">
    </div>
    <div class="col-xs-12">
      <h3>Product Name: {{product.name}}</h3>
    </div>
    <div class="col-xs-12">
      <h5>Product Description: {{product.description}}</h5>
    </div>
    <div class="col-xs-4">
        <h4>Quantity: <input type="number" name="quantity" min="1" max="999" step="1" value="1"></h4>
    </div>
    <div class="col-xs-4">
      <h4>Price: <span id="qty-price" data-price="{{product.price}}">{{product.price}}</span></h4>
    </div>
</div>

<div class="modal-footer">
  <button class="btn btn-success" type="button" name="button"> <span class="glyphicon glyphicon-shopping-cart">+</span></button>
  <button type="button" class="close" data-dismiss="modal" aria-label="Close">
</div>
